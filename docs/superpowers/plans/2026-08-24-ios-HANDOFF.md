# FitrahTube iOS — Session Handoff (2026-08-24 ~16:10, PAUSED)

> **Owner directive 2026-08-27 (superseding):** the `openInYouTube` rung described below was REMOVED — the app never offers any hand-off to YouTube (RULINGS.md Q75). Historical text left as written.

Read this first on the next session. It is the single source of "where we are / what's next". Trust it plus `git log` and the SDD ledger over any recollection.

**Branch:** `feature/ios-app` (off `develop`). **Nothing pushed.** No PR opened.
**HEAD at pause:** `f102206b` `[FEAT]: iOS player metadata and toolbar`.
**Working tree:** clean except ONE untracked file that is **NOT ours** — `docs/superpowers/plans/2026-08-23-ios-phase2a-innertubekit.md` (a peer session's duplicate InnerTubeKit plan). **Never stage it.**
**Session state:** truly idle — no agents, no builds running. Resume only on the user's word.

---

## Progress: ~45% of the full app

| Phase | What | Status | ~% of app |
|---|---|---|---|
| 0 | Scaffold (XcodeGen, generated FitrahAPI client, DI container, design tokens) | ✅ shipped + gated | 8 |
| 1 | Catalog UI (11 screens, en/ar/nl, RTL, Dynamic Type, iPad rail) | ✅ shipped + full 9-stage pipeline | 22 |
| 2A | InnerTubeKit (stream/browse engine) | ✅ shipped, proven vs live YouTube | 10 |
| 2B1 | Player core | 🔄 **8 of 10 tasks done** | ~5 of 8 |
| 2B2–B5, C | Player extras + detail screens + share/report + RemoteConfig UI | ⬜ not started | ~10 |
| 3 | Downloads, Chromecast, AirPlay, dub audio | ⬜ | 15 |
| 4 | Accounts (auth, sync, import, age gate, deletion) | ⬜ | 12 |
| 5 | Backend additions (existing Spring app) | ⬜ | 5 |
| 6 | App Store readiness | ⬜ (user-blocked items) | 8 |

---

## What's DONE in detail

### Phase 0 + Phase 1 — COMPLETE & GATED
- Both passed the mandatory 9-stage review pipeline. Phase 1 closed at commit `070cc129`; CHANGELOG entry `ba5f44e7`. SDD workspaces for these were archived to the session scratchpad and deleted.
- Key infra that everything reuses: `@MainActor final class AppContainer` (composition-root DI, `@Environment(\.container)`, `fake()` for DEBUG/previews); `Localizable.xcstrings` generated by `ios/scripts/convert-strings.py` from Android strings (R7 fallback amended: absent locale → `needs_review`+English, loanwords → `translated`); `Tokens.swift` (`WidthClass` on smallest width, `Spacing`, `Radius`, `TypeScale`, `Format` = ICU compact + plurals); XCUITest screenshot rig `ios/scripts/screenshots.sh` + `ScreenshotTests.swift` + `-fitrah-*` launch hooks; test gate `ios/scripts/test.sh` (run from REPO ROOT: drift check → xcodegen → xcodebuild test on iPhone 17 + iPad Pro 13" (M5) → package `swift test` → Release build; 300 s watchdog).

### Phase 2A — InnerTubeKit — SHIPPED (`ios/Packages/InnerTubeKit`)
- Pure-Swift stream-resolution + browse engine. 16 source files, 86 tests, zero warnings. Commits `7440d186..af2cd2b5`. Final whole-branch review → SHIP.
- **Proven against LIVE YouTube**: `LiveResolveTests` (env-gated `INNERTUBE_LIVE=1`, off by default) resolves a real video to HLS.
- **The one bug that mattered, fixed:** the first tokenless `player` POST is *always* bot-checked by YouTube, and that bot-check response carries the `visitorData` to reuse. Capturing it only on `.ok` made the ladder loop tokenless and demote to 360p. Now the bot-check's visitor is adopted + retried. Plan §6.2/§6.3 corrected (commit `729aca54`).
- Anti-bot hardening (final-gate fixes): concurrency-safe ≥500ms POST spacing (reserve-before-await); persisted escalating cooldown now enforced (`ExtractionError.cooldown(until:)`, terminal; `InnerTube.cooldownRemaining()`).
- Public surface the app consumes: `InnerTube` (composition root) → `resolver` (`StreamResolver`), `browse` (`BrowseClient`), `atom` (`AtomFeedFetcher`), `rateLimiter`, `remoteConfig`. Models: `Resolved`/`ResolvedStream` (`.hls`/`.progressive`/`.embed`/`.openInYouTube`), `ExtractionError`, `CaptionTrack`, `VideoItem`, `Purpose`.
- **Carry-forwards for consumers:** `docs/superpowers/plans/2026-08-23-ios-phase2-research/PHASE2-CARRYFORWARDS.md` (CF-B1..6 for the player, CF-C1..3 for detail screens). Notably: app must supply the `AvailabilityGate` (done in B1 T1); `BrowseClient.channelTab(.shorts/.playlists)` returns EMPTY — the Shorts/Playlists `lockupViewModel` variants are unmodelled and Plan C must model them (channel-detail.md has an addendum).

### Phase 2B1 — Player core — Tasks 1–8 of 10 DONE
Plan: `docs/superpowers/plans/2026-08-24-ios-phase2b1-player-core.md`. Ledger: `.superpowers/sdd/2026-08-24-ios-phase2b1-player-core/progress.md`. All tasks reviewed + fix-rounds closed except Task 8 (committed, review pending — see below).

| # | Task | Commit(s) | Notes |
|---|---|---|---|
| 1 | Availability gate + InnerTube in container | `42376df1` | `BackendAvailabilityGate` (HEAD /videos|channels/{id}; 2xx/404→ok, 410→unavailable, transport-err→fail-open). Fixed an InnerTubeKit `Package.swift` bundle-resource bug that only app-linking surfaced. |
| 2 | `PlayerViewModel` state machine | `9f03e983` | `StreamState` enum; resolver-outcome→state mapping (exhaustive); generation guard; `.cooldown` never retries into itself. Pure/fake-resolver tested. |
| 3 | `AVPlayerViewController` host + rung-1 playback | `145dda2a` | Real decoded frame captured under test. **SDK notes:** use `AVURLAssetHTTPUserAgentKey` (the HeaderFieldsKey was removed in iOS 26.2 SDK); AVKit stock controls are NOT XCUITest-accessible — anchor on our own SwiftUI controls. |
| 4 | Quality ceiling (+ fix round) | `1299842e`,`07b4fa16` | `preferredMaximumResolution`(PIXELS — ×displayScale) + `preferredPeakBitRate`; `…ForExpensiveNetworks` backstop; hidden on rung-2; own SwiftUI menu. |
| 5 | Audio-language sticky menu | `43aec06c` | `mediaSelectionGroup(.audible)`; sticky per session; hidden when ≤1 track. |
| 6 | Captions (+ fix round) | `2299a26e`,`05d0e3f8` | WebVTT parser + overlay; menu filters `\.isAutoGenerated` (manual tracks ride AVKit's stock menu). |
| 7 | Recovery + rung-2 pill (+ fix round) | `dc4562c8`,`0fe8d81b` | Pure `StallWatchdog` (fires ONLY while `timeControlStatus == .waitingToPlayAtSpecifiedRate`); budgets retries 3 / re-resolves 2; `isRecovering` serialises events; `.ready`/`.rung2Progressive` share ONE switch branch (host identity — do not split). |
| 8 | Metadata + toolbar | `f102206b` | Metadata (title/channel/views via `Format`/description link allow-list) + toolbar (favorite optimistic, Share watch-URL, Report **inert no-op**, Download hidden). **Review NOT yet run.** |

---

## RESUME CHECKLIST (do these in order)

1. **Verify tree + reorient:** `git log --oneline -12`; confirm HEAD `f102206b`; read `.superpowers/sdd/2026-08-24-ios-phase2b1-player-core/progress.md` tail.
2. **Capture the deferred screenshot:** run `ios/scripts/screenshots.sh "iPhone 17"` for the `b1-task8` metadata/toolbar PNG and eyeball it (the Task-8 agent committed without invoking it, per a commit-only instruction).
3. **Review Task 8** (metadata + toolbar): package `0fe8d81b..f102206b`, dispatch a task reviewer. Judge two disclosed choices: (a) the "Show more/less" description toggle always renders (no SwiftUI truncation API — ponytail-noted); (b) the **Report button is an inert no-op** — decide whether B1 should show a "coming soon" affordance or leave it for Plan C, which wires the real report flow. Run any resulting fix round.
4. **Task 9 — Player state UI** (per-rung states + VoiceOver). MUST carry:
   - **T2-1:** add `player_error_generic` to the catalog via `convert-strings.py` EXTRA_KEYS (the VM uses it for `.embed`/`.openInYouTube` today; drift check must stay green), OR swap the VM to the existing `player_error_message`.
   - **T7-M3 (required):** `.recoveryExhausted` currently renders a bare `ProgressView()` — give it the manual **Retry** escape hatch. It is a reachable dead end until Task 9 fixes it.
   - **Host identity constraint (LOAD-BEARING):** keep `.ready` and `.rung2Progressive` in ONE switch branch; any "re-resolving…" affordance must be an OVERLAY, not a state hop, or SwiftUI rebuilds the host and drops the playback position.
   - States per spec §6.6: `.loading` (thumbnail+spinner, offline-gated by `NetworkMonitor`), `.rung2Progressive` (pill — done in T7), `.error` (+Retry), `.contentUnavailable` (no retry), `.cooldown(until:)` (countdown from `InnerTube.cooldownRemaining()`), `.recoveryExhausted` (Retry). Each rung transition posts `AccessibilityNotification.Announcement`; cross-dissolve, static under Reduce Motion.
5. **Task 10 — iPad/RTL/Dynamic-Type/VoiceOver pass** closes B1 (spec §6.11; player 16:9 top-anchored to a max width on iPad; compact-height landscape hides metadata).
6. **B1 final whole-branch review** (opus, adversarial) → SHIP; then write the carry-forwards for B2–B5.
7. **Then Plan B2** (background audio/Now Playing/PiP), **B3** (embed rung + Safe Mode), **B4** (Shorts), **B5** (fullscreen/gestures/Up Next queue). Each is decomposed in `.superpowers/sdd/scratch/plan-B-decomposition.md`. Then **Plan C** (channel/playlist detail, share/report, RemoteConfig UI — must model the Shorts/Playlists `lockupViewModel` variants, CF-C1). Then the **Phase 2 gate** (9-stage pipeline).

---

## OPERATIONAL RULES (learned this session — do not relearn the hard way)
- **Serialise implementers.** Two concurrent `test.sh`/xcodebuild gates collide on shared `ios/DerivedData` (`build.db is locked`, phantom test kills). Reviews (read-only) may run in parallel with one implementer.
- **iOS 26.2 SDK quirks:** `AVURLAssetHTTPUserAgentKey` not the removed `…HTTPHeaderFieldsKey`; AVKit's own transport chrome is not XCUITest-queryable — build our own SwiftUI controls and anchor screenshots/tests on `player.*` a11y identifiers.
- **Subagent hygiene:** every dispatch says "stage specific files only, never `git add -A`" and names the untracked peer doc to skip. Verify commit authorship + `--stat` file list each task.
- **SourceKit diagnostics** ("No such module 'InnerTubeKit'/'Testing'/'UIKit'") are LSP indexer noise (no generated xcodeproj context) — the `test.sh` gate is the truth. Ignore them.
- **Model/effort:** Fable 5 for the controller (rulings, plan-writing, consolidation); Sonnet for most implementers/reviewers, Opus for keystones (StreamResolver, recovery) and final whole-branch gates.

## USER-BLOCKED (needed before Phase 6 / shipping; none block the code work)
Apple Team ID · Firebase iOS `GoogleService-Info.plist` (→ `~/.config/albunyaan/`, never commit) · 1024px icon master · confirm About link hosts (`albunyaan.tube`) · go-ahead to push + open the Phase 0/1 PR into `develop`.

## KEY PATHS
- Player plan: `docs/superpowers/plans/2026-08-24-ios-phase2b1-player-core.md` · ledger `.superpowers/sdd/2026-08-24-ios-phase2b1-player-core/progress.md` · deferred minors `.../deferred-minors.md`
- Phase 2 research + rulings: `docs/superpowers/plans/2026-08-23-ios-phase2-research/` (RULINGS.md Q1–Q74, INDEX.md, PHASE2-CARRYFORWARDS.md, per-subsystem briefs)
- Plan B decomposition: `.superpowers/sdd/scratch/plan-B-decomposition.md`
- Spec: `docs/superpowers/specs/2026-08-23-ios-app-design.md` (§9 InnerTubeKit, §10 player). App plan: `docs/architecture/ios-app-plan.md` (§6.x).
- Memory: `~/.claude/projects/-Users-farouqabouumar-Development-albunyaantube/memory/ios-app-effort-status.md`
