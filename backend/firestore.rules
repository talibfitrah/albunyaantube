rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all
    match /{document=**} {
      allow read, write: if false;
    }

    // Allow server-side access via Firebase Admin SDK
    // This file is just a placeholder for firebase.json
    // Actual security is handled by Spring Security in the backend
  }
}
