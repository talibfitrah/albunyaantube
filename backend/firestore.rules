rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all access for Firebase Admin SDK and emulator testing
    // When using Firebase Admin SDK, these rules are bypassed in production
    // For emulator, we allow all to support integration tests
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

