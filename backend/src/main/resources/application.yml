spring:
  application:
    name: albunyaan-tube
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/albunyaan
    username: albunyaan
    password: changeme
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379
      ssl: false
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info
app:
  security:
    jwt:
      secret: s5fQPZOwtMVQXVPyiKSTdlEPq8GMMd2CgrDeCB6Nekk=
      access-token-ttl-seconds: 900
      refresh-token-ttl-seconds: 259200
