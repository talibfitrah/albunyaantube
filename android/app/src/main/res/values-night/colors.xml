<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark Mode Colors (Design Spec) -->
    <color name="primary_green">#35C491</color>
    <color name="primary_variant">#35C491</color>
    <color name="surface_variant">#1A2E27</color>
    <color name="accent_red">#EF5350</color>

    <!-- Material aliases -->
    <color name="primary">@color/primary_green</color>
    <color name="primaryContainer">@color/primary_variant</color>
    <color name="surfaceVariant">@color/surface_variant</color>
    <color name="onPrimary">#FFFFFFFF</color>

    <!-- Filter chip palette (dark mode) -->
    <color name="filter_chip_default_bg">#1A2E27</color>
    <color name="filter_chip_default_text">#35C491</color>
    <color name="filter_chip_selected_bg">#35C491</color>
    <color name="filter_chip_selected_text">#0A1F18</color>

    <!-- Home Screen Design System (dark) -->
    <color name="home_surface_background">#0F1512</color>
    <color name="home_card_background">#1A231F</color>
    <color name="home_category_pill_bg">#12352B</color>
    <color name="home_text_primary">#F1F5F9</color>
    <color name="home_text_secondary">#9CB3A7</color>
    <color name="home_text_muted">#74847C</color>
    <color name="home_duration_bg">#CC000000</color>
    <color name="home_video_count_bg">#CC35C491</color>

    <!-- Icon colors (adaptive for light/dark) -->
    <color name="ic_icon_default">#DEFFFFFF</color>

    <!-- Home error state colors (dark mode) -->
    <color name="home_error_bg">#3D2A1A</color>
    <color name="home_error_text">#FFCC80</color>
    <color name="home_error_icon">#FFB74D</color>

    <!-- Home empty state colors (dark mode) -->
    <color name="home_empty_bg">@color/home_card_background</color>
    <color name="home_empty_text">@color/home_text_secondary</color>
    <color name="home_empty_icon">@color/home_text_muted</color>

    <!-- Hero overlays for playlist/channel detail (stronger in dark mode) -->
    <color name="hero_overlay">#66000000</color>

    <!-- Additional colors (dark mode overrides) -->
    <color name="background_gray">#121212</color>
    <color name="icon_gray">#9E9E9E</color>

    <!-- Skeleton/Loading colors (dark mode) -->
    <color name="skeleton_background">#2A2A2A</color>
    <color name="skeleton_shimmer_start">#2A2A2A</color>
    <color name="skeleton_shimmer_end">#383838</color>

    <!-- Shimmer for loading skeletons (dark mode) -->
    <color name="shimmer_background_color">#2A2A2A</color>

    <!-- Navigation Rail/Bottom Nav colors (dark mode) -->
    <color name="nav_item_inactive">#B0B0B0</color>  <!-- Light gray for dark mode visibility -->
    <color name="nav_item_focused">#E0E0E0</color>  <!-- Brighter when focused (more prominent in dark mode) -->
    <color name="nav_item_inactive_tv">#D0D0D0</color>  <!-- Even lighter for TV 10-foot viewing in dark mode -->
    <color name="nav_item_focused_tv">#F0F0F0</color>  <!-- Focused state for TV in dark mode -->
    <color name="nav_item_selected_focused_tv">#5BDEAE</color>  <!-- Brighter mint for selected+focused on TV (D-pad highlight in dark mode) -->

    <!-- Settings icon backgrounds (theme-aware, dark mode) -->
    <color name="settings_icon_bg">#2A3530</color>
</resources>
