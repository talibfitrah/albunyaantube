<?xml version="1.0" encoding="utf-8"?>
<!-- Tablet layout for Playlist Detail screen (sw600dp) -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- AppBarLayout with collapsing toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:fitsSystemWindows="true">

        <!-- Collapsing content: Toolbar + Banner + Playlist Info -->
        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsingToolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?attr/actionBarSize"
            android:fitsSystemWindows="true"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:contentScrim="?attr/colorSurface"
            app:titleEnabled="false"
            app:toolbarId="@id/toolbar">

            <!-- Scrollable Header Content -->
            <LinearLayout
                android:id="@+id/headerContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layoutDirection="locale"
                android:orientation="vertical"
                app:layout_collapseMode="parallax"
                app:layout_collapseParallaxMultiplier="0.5">

                <!-- Exclusion Banner -->
                <TextView
                    android:id="@+id/exclusionBanner"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="@dimen/spacing_sm"
                    android:background="@color/accent_red"
                    android:textColor="@android:color/white"
                    android:text="@string/detail_excluded_banner"
                    android:textAlignment="viewStart"
                    android:visibility="gone"
                    tools:visibility="visible" />

                <!-- Playlist Hero (YouTube-style: blurred background + centered thumbnail) -->
                <FrameLayout
                    android:id="@+id/playlistHeroContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">

                    <!-- Blurred Background Image (fills width, softened) -->
                    <ImageView
                        android:id="@+id/heroBackgroundBlurred"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/playlist_hero_height"
                        android:scaleType="centerCrop"
                        android:alpha="0.6"
                        android:background="@drawable/thumbnail_placeholder"
                        android:contentDescription="@null"
                        android:importantForAccessibility="no"
                        tools:src="@drawable/thumbnail_placeholder" />

                    <!-- Color overlay for better contrast -->
                    <View
                        android:id="@+id/heroColorOverlay"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/playlist_hero_height"
                        android:background="@color/hero_overlay"
                        android:contentDescription="@null"
                        android:importantForAccessibility="no" />

                    <!-- Bottom gradient for text overlay readability (bottom-to-top gradient) -->
                    <View
                        android:id="@+id/bannerGradient"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/playlist_hero_height"
                        android:background="@drawable/banner_gradient_overlay"
                        android:contentDescription="@null"
                        android:importantForAccessibility="no" />

                    <!-- Top gradient scrim for toolbar readability (rendered on top for visibility) -->
                    <View
                        android:id="@+id/bannerGradientTop"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/banner_gradient_top_height"
                        android:background="@drawable/banner_gradient_top"
                        android:contentDescription="@null"
                        android:importantForAccessibility="no" />

                    <!-- Centered 16:9 Thumbnail Card (foreground) -->
                    <androidx.constraintlayout.widget.ConstraintLayout
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/playlist_hero_height"
                        android:layout_gravity="center">

                        <!-- Thumbnail card: 60% of container width, capped at max width on large tablets.
                             ConstraintLayout applies the more restrictive constraint, ensuring the card
                             scales proportionally on tablets while not exceeding a readable size. -->
                        <com.google.android.material.card.MaterialCardView
                            android:id="@+id/heroThumbnailCard"
                            android:layout_width="0dp"
                            android:layout_height="0dp"
                            android:layout_margin="@dimen/spacing_lg"
                            app:cardCornerRadius="@dimen/home_thumbnail_corner_radius"
                            app:cardElevation="@dimen/home_card_elevation"
                            app:layout_constraintBottom_toBottomOf="parent"
                            app:layout_constraintDimensionRatio="16:9"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            app:layout_constraintWidth_max="@dimen/playlist_hero_thumbnail_max_width"
                            app:layout_constraintWidth_percent="0.6">

                            <com.google.android.material.imageview.ShapeableImageView
                                android:id="@+id/heroThumbnail"
                                android:layout_width="match_parent"
                                android:layout_height="match_parent"
                                android:scaleType="centerCrop"
                                android:background="@drawable/thumbnail_placeholder"
                                android:contentDescription="@string/cd_playlist_banner"
                                tools:src="@drawable/thumbnail_placeholder" />

                        </com.google.android.material.card.MaterialCardView>

                    </androidx.constraintlayout.widget.ConstraintLayout>

                    <!-- Legacy playlistBanner ID for backward compatibility -->
                    <ImageView
                        android:id="@+id/playlistBanner"
                        android:layout_width="0dp"
                        android:layout_height="0dp"
                        android:visibility="gone"
                        tools:ignore="ContentDescription" />

                </FrameLayout>

                <!-- Playlist Info Header (Tablet: more horizontal spacing) -->
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingStart="@dimen/playlist_header_horizontal_margin"
                    android:paddingEnd="@dimen/playlist_header_horizontal_margin"
                    android:paddingTop="@dimen/spacing_lg"
                    android:paddingBottom="@dimen/spacing_lg">

                    <!-- Playlist Title (Tablet: larger text) -->
                    <TextView
                        android:id="@+id/playlistTitle"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:textSize="@dimen/text_headline"
                        android:textStyle="bold"
                        android:textColor="?attr/colorOnSurface"
                        android:textAlignment="viewStart"
                        android:maxLines="2"
                        android:ellipsize="end"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        tools:text="Quran Recitations by Mishary Alafasy" />

                    <!-- Channel Name (tappable) -->
                    <TextView
                        android:id="@+id/channelName"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/spacing_xs"
                        android:textSize="@dimen/text_body"
                        android:textColor="@color/primary_green"
                        android:textAlignment="viewStart"
                        android:clickable="true"
                        android:focusable="true"
                        android:background="?attr/selectableItemBackground"
                        android:visibility="gone"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toBottomOf="@id/playlistTitle"
                        tools:text="Mishary Rashid Alafasy"
                        tools:visibility="visible" />

                    <!-- Playlist Metadata (video count, duration) -->
                    <TextView
                        android:id="@+id/playlistMetadata"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/spacing_xs"
                        android:textSize="@dimen/text_body"
                        android:textColor="?attr/colorOnSurfaceVariant"
                        android:textAlignment="viewStart"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toBottomOf="@id/channelName"
                        tools:text="42 videos" />

                    <!-- Category Chip -->
                    <com.google.android.material.chip.ChipGroup
                        android:id="@+id/categoryChipsContainer"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/spacing_sm"
                        android:visibility="gone"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toBottomOf="@id/playlistMetadata"
                        tools:visibility="visible">

                        <com.google.android.material.chip.Chip
                            android:id="@+id/categoryChip"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:clickable="false"
                            tools:text="Quran" />

                    </com.google.android.material.chip.ChipGroup>

                    <!-- Actions Row (Tablet: horizontal layout) -->
                    <LinearLayout
                        android:id="@+id/actionsContainer"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/spacing_lg"
                        android:orientation="horizontal"
                        android:weightSum="3"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toBottomOf="@id/categoryChipsContainer">

                        <!-- Play All Button -->
                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/playAllButton"
                            android:layout_width="0dp"
                            android:layout_height="@dimen/touch_target_button"
                            android:layout_weight="1"
                            android:layout_marginEnd="@dimen/spacing_sm"
                            android:text="@string/playlist_play_all"
                            android:contentDescription="@string/cd_play_all_button"
                            app:icon="@drawable/ic_play"
                            app:iconGravity="textStart" />

                        <!-- Shuffle Button -->
                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/shuffleButton"
                            style="@style/Widget.Material3.Button.OutlinedButton"
                            android:layout_width="0dp"
                            android:layout_height="@dimen/touch_target_button"
                            android:layout_weight="1"
                            android:layout_marginStart="@dimen/spacing_sm"
                            android:layout_marginEnd="@dimen/spacing_sm"
                            android:text="@string/playlist_shuffle"
                            android:contentDescription="@string/cd_shuffle_button"
                            app:icon="@drawable/ic_shuffle"
                            app:iconGravity="textStart" />

                        <!-- Download Button -->
                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/downloadPlaylistButton"
                            android:layout_width="0dp"
                            android:layout_height="@dimen/touch_target_button"
                            android:layout_weight="1"
                            android:layout_marginStart="@dimen/spacing_sm"
                            android:text="@string/playlist_detail_download"
                            android:contentDescription="@string/cd_download_playlist_button"
                            app:icon="@drawable/ic_download"
                            app:iconGravity="textStart" />

                    </LinearLayout>

                    <!-- Download Policy Text -->
                    <TextView
                        android:id="@+id/downloadPolicyText"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/spacing_sm"
                        android:textSize="@dimen/text_caption"
                        android:textColor="?attr/colorOnSurfaceVariant"
                        android:textAlignment="viewStart"
                        android:text="@string/playlist_detail_download_hint"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toBottomOf="@id/actionsContainer" />

                </androidx.constraintlayout.widget.ConstraintLayout>

            </LinearLayout>

            <!-- Toolbar with back button - pins when collapsed -->
            <!-- Design: White icons over banner with 50% black gradient scrim (banner_gradient_top.xml)
                 providing WCAG AA compliant contrast. Not using theme attrs as toolbar must remain
                 visible over dynamic banner images. -->
            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@android:color/transparent"
                app:layout_collapseMode="pin"
                app:navigationIcon="@drawable/ic_arrow_back"
                app:navigationIconTint="@android:color/white"
                app:titleTextColor="@android:color/white" />

        </com.google.android.material.appbar.CollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Content Container with RecyclerView, Skeleton, and Error states -->
    <FrameLayout
        android:id="@+id/contentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layoutDirection="locale"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Constrained width container for tablet -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_horizontal"
            android:paddingStart="@dimen/playlist_header_horizontal_margin"
            android:paddingEnd="@dimen/playlist_header_horizontal_margin">

            <!-- Main Content (Videos list) -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/videosRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:visibility="gone"
                tools:visibility="visible"
                tools:listitem="@layout/item_video_list" />

            <!-- List Skeleton (shown while loading items) -->
            <LinearLayout
                android:id="@+id/listSkeletonContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                tools:visibility="visible">

                <include layout="@layout/skeleton_content_item" />
                <include layout="@layout/skeleton_content_item" />
                <include layout="@layout/skeleton_content_item" />
                <include layout="@layout/skeleton_content_item" />
                <include layout="@layout/skeleton_content_item" />

            </LinearLayout>

            <!-- Empty State -->
            <include
                android:id="@+id/emptyState"
                layout="@layout/empty_state"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />

            <!-- Error State -->
            <include
                android:id="@+id/errorState"
                layout="@layout/error_state"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />

        </FrameLayout>

    </FrameLayout>

    <!-- Header Skeleton (shown while loading header) - overlays AppBarLayout -->
    <LinearLayout
        android:id="@+id/headerSkeleton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="?attr/actionBarSize"
        android:orientation="vertical"
        android:background="?attr/colorSurface"
        android:elevation="@dimen/elevation_lg"
        android:visibility="gone"
        tools:visibility="visible">

        <!-- Banner skeleton (matches hero height to prevent layout shift) -->
        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/playlist_hero_height"
            android:background="@drawable/skeleton_shimmer" />

        <!-- Title and metadata skeleton -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/playlist_header_horizontal_margin"
            android:paddingEnd="@dimen/playlist_header_horizontal_margin"
            android:paddingTop="@dimen/spacing_lg"
            android:paddingBottom="@dimen/spacing_lg">

            <!-- Title skeleton -->
            <View
                android:id="@+id/skeletonTitle"
                android:layout_width="0dp"
                android:layout_height="28dp"
                android:background="@drawable/skeleton_shimmer"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintWidth_percent="0.6" />

            <!-- Channel skeleton -->
            <View
                android:id="@+id/skeletonChannel"
                android:layout_width="180dp"
                android:layout_height="18dp"
                android:layout_marginTop="@dimen/spacing_sm"
                android:background="@drawable/skeleton_shimmer"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/skeletonTitle" />

            <!-- Metadata skeleton -->
            <View
                android:id="@+id/skeletonMetadata"
                android:layout_width="120dp"
                android:layout_height="18dp"
                android:layout_marginTop="@dimen/spacing_xs"
                android:background="@drawable/skeleton_shimmer"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/skeletonChannel" />

            <!-- Button skeleton row (horizontal) -->
            <LinearLayout
                android:id="@+id/skeletonPlayAllButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_lg"
                android:orientation="horizontal"
                android:weightSum="3"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/skeletonMetadata">

                <View
                    android:layout_width="0dp"
                    android:layout_height="@dimen/touch_target_button"
                    android:layout_weight="1"
                    android:layout_marginEnd="@dimen/spacing_sm"
                    android:background="@drawable/skeleton_shimmer" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="@dimen/touch_target_button"
                    android:layout_weight="1"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:layout_marginEnd="@dimen/spacing_sm"
                    android:background="@drawable/skeleton_shimmer" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="@dimen/touch_target_button"
                    android:layout_weight="1"
                    android:layout_marginStart="@dimen/spacing_sm"
                    android:background="@drawable/skeleton_shimmer" />

            </LinearLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
