<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/onboarding_padding">

    <!-- Help Button - floats at top right -->
    <ImageButton
        android:id="@+id/helpButton"
        android:layout_width="@dimen/touch_target_min"
        android:layout_height="@dimen/touch_target_min"
        android:layout_margin="0dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/onboarding_help"
        android:src="@android:drawable/ic_menu_help"
        android:tint="@color/home_text_primary"
        android:focusable="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ViewPager2 for swipeable pages - takes most of the screen -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="@string/onboarding_carousel_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/indicatorRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Dot indicators -->
    <LinearLayout
        android:id="@+id/indicatorRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="@dimen/spacing_lg"
        app:layout_constraintBottom_toTopOf="@id/primaryCta"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <View
            android:id="@+id/indicator1"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_margin="6dp"
            android:background="@drawable/onboarding_indicator_active"/>

        <View
            android:id="@+id/indicator2"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_margin="6dp"
            android:background="@drawable/onboarding_indicator_inactive"/>

        <View
            android:id="@+id/indicator3"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_margin="6dp"
            android:background="@drawable/onboarding_indicator_inactive"/>
    </LinearLayout>

    <!-- Primary CTA Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/primaryCta"
        android:layout_width="0dp"
        android:layout_height="@dimen/onboarding_button_height"
        android:layout_marginHorizontal="@dimen/spacing_lg"
        android:maxWidth="@dimen/onboarding_button_max_width"
        android:text="@string/onboarding_continue"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/primary_green"
        android:focusable="true"
        android:nextFocusDown="@id/skipButton"
        android:nextFocusUp="@id/viewPager"
        app:cornerRadius="@dimen/onboarding_button_corner_radius"
        app:layout_constraintWidth_max="@dimen/onboarding_button_max_width"
        app:layout_constraintBottom_toTopOf="@id/skipButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="@dimen/spacing_md"/>

    <!-- Skip Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/skipButton"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_lg"
        android:text="@string/onboarding_skip"
        android:textColor="@color/home_text_secondary"
        android:focusable="true"
        android:nextFocusUp="@id/primaryCta"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
