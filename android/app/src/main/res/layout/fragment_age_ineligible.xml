<?xml version="1.0" encoding="utf-8"?>
<!--
  Plan C T9 + T13 polish: Age-ineligible terminal screen — phone variant (<600dp).

  Structure mirrors the sw600dp/sw720dp variants:
    outer FrameLayout owns fitsSystemWindows="true" (status/nav bar insets)
    inner ConstraintLayout owns android:padding (horizontal margins)

  This is required on Android 15 edge-to-edge (SDK 35): a single view cannot
  carry BOTH android:padding AND fitsSystemWindows="true" — the framework's
  default inset dispatch calls View.setPadding(left, top, right, bottom) with
  the system-bar insets and OVERWRITES the explicit padding, dropping the
  horizontal margins entirely.

  Same view IDs across all three variants so the Fragment binds the same code.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.albunyaan.tube.ui.bootstrap.AgeIneligibleFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="@dimen/spacing_lg"
        android:paddingEnd="@dimen/spacing_lg"
        android:paddingTop="@dimen/spacing_lg"
        android:paddingBottom="@dimen/spacing_lg">

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/age_ineligible_title"
            android:textAppearance="?attr/textAppearanceHeadlineSmall"
            android:textAlignment="center"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/body"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintVertical_chainStyle="packed" />

        <TextView
            android:id="@+id/body"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:text="@string/age_ineligible_body"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:textAlignment="center"
            app:layout_constraintTop_toBottomOf="@id/title"
            app:layout_constraintBottom_toTopOf="@id/okButton"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/okButton"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="@dimen/auth_button_height"
            android:layout_marginTop="@dimen/spacing_lg"
            android:text="@string/age_ineligible_ok_button"
            app:layout_constraintTop_toBottomOf="@id/body"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</FrameLayout>
