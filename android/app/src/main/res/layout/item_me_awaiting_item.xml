<?xml version="1.0" encoding="utf-8"?>
<!--
  B14: A single awaiting-import item row in the Me tab.
  Compact horizontal layout: 56dp thumbnail | title + "Pending review" chip.
  NOT clickable — tap does nothing (items are awaiting admin approval).
  RTL-safe: textAlignment="viewStart".
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="false"
    android:focusable="false"
    android:paddingHorizontal="@dimen/spacing_md"
    android:paddingVertical="@dimen/spacing_sm">

    <!-- Thumbnail / avatar placeholder -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/awaitingItemThumbnailCard"
        android:layout_width="56dp"
        android:layout_height="56dp"
        app:cardBackgroundColor="?attr/colorSurfaceVariant"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:strokeWidth="0dp">

        <ImageView
            android:id="@+id/awaitingItemThumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop" />

    </com.google.android.material.card.MaterialCardView>

    <!-- Title -->
    <TextView
        android:id="@+id/awaitingItemTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="2"
        android:textAlignment="viewStart"
        android:textColor="?attr/colorOnSurface"
        android:textSize="@dimen/text_body"
        app:layout_constraintBottom_toTopOf="@+id/awaitingItemChip"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/awaitingItemThumbnailCard"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginStart="@dimen/spacing_md" />

    <!-- "Pending review" chip -->
    <com.google.android.material.chip.Chip
        android:id="@+id/awaitingItemChip"
        style="@style/Widget.Material3.Chip.Assist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_md"
        android:layout_marginTop="@dimen/spacing_xs"
        android:checkable="false"
        android:clickable="false"
        android:focusable="false"
        android:textSize="@dimen/text_caption"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/awaitingItemThumbnailCard"
        app:layout_constraintTop_toBottomOf="@+id/awaitingItemTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>
