<?xml version="1.0" encoding="utf-8"?>
<!--
  B14: "Imported — awaiting review" section header for the Me tab.
  Shows the section title and a total count badge side-by-side.
  RTL-safe: textAlignment="viewStart", no gravity="left".
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="@dimen/spacing_md"
    android:paddingTop="@dimen/spacing_md"
    android:paddingBottom="@dimen/spacing_sm">

    <TextView
        android:id="@+id/awaitingHeaderTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textAlignment="viewStart"
        android:textColor="?attr/colorOnSurface"
        android:textSize="@dimen/text_section_title"
        android:textStyle="bold" />

    <com.google.android.material.chip.Chip
        android:id="@+id/awaitingHeaderCount"
        style="@style/Widget.Material3.Chip.Assist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:checkable="false"
        android:clickable="false"
        android:focusable="false"
        android:textSize="@dimen/text_caption" />

</LinearLayout>
