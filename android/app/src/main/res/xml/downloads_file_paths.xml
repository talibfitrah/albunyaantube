<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path
        name="downloads"
        path="downloads/" />
    <!-- ANDROID-MULTI-01 Issue 3: expose cacheDir/updates so the system package
         installer can read the downloaded APK via FileProvider. -->
    <cache-path
        name="updates"
        path="updates/" />
    <cache-path
        name="share_cache"
        path="share_cache/" />
</paths>
