<?xml version="1.0" encoding="utf-8"?>
<!--
  Branded background for PopupMenus app-wide. Matches the design system's
  20 dp corner radius (corner_radius_large) and uses ?attr/colorSurface
  so the menu adapts to light/dark theme. Without this, PopupMenu falls
  back to Material3's default 4 dp corner background, which looks out of
  place next to the app's 20 dp cards and buttons.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="@dimen/corner_radius_large" />
    <padding
        android:left="@dimen/spacing_xs"
        android:right="@dimen/spacing_xs"
        android:top="@dimen/spacing_xs"
        android:bottom="@dimen/spacing_xs" />
</shape>
