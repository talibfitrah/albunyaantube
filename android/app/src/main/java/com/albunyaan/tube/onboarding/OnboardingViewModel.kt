package com.albunyaan.tube.onboarding

import androidx.lifecycle.ViewModel

/**
 * Placeholder ViewModel representing onboarding state. The real implementation will:
 * - Expose carousel pages backed by localized resources.
 * - Persist completion flag via DataStore (`onboarding_completed`).
 * - Handle help modal visibility and analytics events.
 */
class OnboardingViewModel : ViewModel()
