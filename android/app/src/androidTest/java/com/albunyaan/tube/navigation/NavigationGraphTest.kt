package com.albunyaan.tube.navigation

import org.junit.Test
import org.junit.Ignore

/**
 * Placeholder instrumentation test definition. Once the Android module exists we
 * will inflate `app_nav_graph.xml` via `NavInflater` and assert that the four main
 * destinations (home/channels/playlists/videos) and the splash→onboarding flow are
 * wired correctly. See docs/testing/test-strategy.md for full acceptance notes.
 */
class NavigationGraphTest {

    @Ignore("Android module not yet initialized")
    @Test
    fun navGraph_containsExpectedDestinations() {
        // TODO: Implement once the Android project is scaffolded.
    }
}
